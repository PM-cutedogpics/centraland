/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['media.karousell.com', 'down-ph.img.susercontent.com', 'lzd-img-global.slatic.net']
    }
}

module.exports = nextConfig
